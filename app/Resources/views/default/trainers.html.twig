{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper" class="home">
        <div id="header">
            <div class="content">
                <h1>Trainers</h1>
            </div>
        </div>
        <div class="container">
            <div class="row">
                {% for item in items %}
                    <a href="{{ path('app_default_trainerdetail', {'slug': item.slug }) }}">
                        <div class="trainerblock col-md-3">
                            <img class="avatar" src="{{ asset('upload/image/'~app.user.avatar) | imagine_filter('avatar_overview') }}">
                            <div class="content">
                                <h3>{{ item.firstName }} {{ item.lastName }}</h3>
                                <p>{{ item.description|length > 150 ? item.description|slice(0, 150) ~ '...' : item.description }}</p>
                            </div>
                        </div>
                    </a>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}

